///
/// HybridMediaStreamTrackSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `MediaStreamTrackState` to properly resolve imports.
namespace margelo::nitro::webrtc { enum class MediaStreamTrackState; }

#include <string>
#include "MediaStreamTrackState.hpp"

namespace margelo::nitro::webrtc {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `MediaStreamTrack`
   * Inherit this class to create instances of `HybridMediaStreamTrackSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridMediaStreamTrack: public HybridMediaStreamTrackSpec {
   * public:
   *   HybridMediaStreamTrack(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridMediaStreamTrackSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridMediaStreamTrackSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridMediaStreamTrackSpec() override = default;

    public:
      // Properties
      virtual std::string getId() = 0;
      virtual std::string getKind() = 0;
      virtual MediaStreamTrackState getReadyState() = 0;
      virtual bool getEnabled() = 0;
      virtual void setEnabled(bool enabled) = 0;
      virtual std::string get_srcPipeId() = 0;
      virtual std::string get_dstPipeId() = 0;

    public:
      // Methods
      virtual void stop() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "MediaStreamTrack";
  };

} // namespace margelo::nitro::webrtc
