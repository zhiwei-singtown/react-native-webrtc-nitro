///
/// HybridWebrtcViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridWebrtcViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::webrtc::views {

  extern const char HybridWebrtcViewComponentName[] = "WebrtcView";

  HybridWebrtcViewProps::HybridWebrtcViewProps(const react::PropsParserContext& context,
                                               const HybridWebrtcViewProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    videoPipeId([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("videoPipeId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.videoPipeId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.videoPipeId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("WebrtcView.videoPipeId: ") + exc.what());
      }
    }()),
    audioPipeId([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("audioPipeId", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.audioPipeId;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.audioPipeId);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("WebrtcView.audioPipeId: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridWebrtcViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridWebrtcViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("WebrtcView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridWebrtcViewProps::HybridWebrtcViewProps(const HybridWebrtcViewProps& other):
    react::ViewProps(),
    videoPipeId(other.videoPipeId),
    audioPipeId(other.audioPipeId),
    hybridRef(other.hybridRef) { }

  bool HybridWebrtcViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("videoPipeId"): return true;
      case hashString("audioPipeId"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridWebrtcViewComponentDescriptor::HybridWebrtcViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridWebrtcViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridWebrtcViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridWebrtcViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridWebrtcViewShadowNode&>(shadowNode);
    const HybridWebrtcViewProps& props = concreteShadowNode.getConcreteProps();
    HybridWebrtcViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::webrtc::views
