///
/// HybridWebrtcViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridWebrtcViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridWebrtcViewSpecSwift.hpp"
#import "Webrtc-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::webrtc;
using namespace margelo::nitro::webrtc::views;

/**
 * Represents the React Native View holder for the Nitro "WebrtcView" HybridView.
 */
@interface HybridWebrtcViewComponent: RCTViewComponentView
@end

@implementation HybridWebrtcViewComponent {
  std::shared_ptr<HybridWebrtcViewSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridWebrtcViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridWebrtcViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridWebrtcViewSpec> hybridView = Webrtc::WebrtcAutolinking::createWebrtcView();
    _hybridView = std::dynamic_pointer_cast<HybridWebrtcViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  Webrtc::HybridWebrtcViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridWebrtcViewProps const>(props);
  auto& newViewProps = const_cast<HybridWebrtcViewProps&>(newViewPropsConst);
  Webrtc::HybridWebrtcViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isRed: boolean
  if (newViewProps.isRed.isDirty) {
    swiftPart.setIsRed(newViewProps.isRed.value);
    newViewProps.isRed.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
